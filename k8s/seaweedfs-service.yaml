apiVersion: v1
kind: Service
metadata:
  name: local-seaweedfs-service
spec:
  type: NodePort  # setting nodePort later is optional
  selector:
    app: seaweedfs
  ports:
    - protocol: TCP
      port: 8333  # port of this service
      targetPort: http-s3  # port on the pod
